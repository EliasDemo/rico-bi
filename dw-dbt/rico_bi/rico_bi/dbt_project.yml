name: 'rico_bi'
version: '1.0.0'

profile: 'rico_bi'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  rico_bi:
    staging:
      +schema: staging
      +materialized: view

    marts:
      +schema: marts
      +materialized: table